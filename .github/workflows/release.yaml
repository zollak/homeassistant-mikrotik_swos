name: Release

on:
  release:
    types: [published]

jobs:
  release:
    name: Build and upload release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7

      - name: Set version in manifest
        run: |
          version="${{ github.event.release.tag_name }}"
          version="${version#v}"
          sed -i "s/\"version\": \".*\"/\"version\": \"${version}\"/" \
            custom_components/mikrotik_swos/manifest.json

      - name: Create zip
        run: |
          cd custom_components/mikrotik_swos
          zip -r ../../mikrotik_swos.zip ./ -x '*__pycache__*' -x '*.pyc'

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          files: mikrotik_swos.zip
